{
	"format_version": "1.8.0",
	"animations": {
		"animation.pufferfish.swim": {
			"loop": true,
			"animation_length": 8,
			"bones": {
				"body": {
					"rotation": [
						"math.sin(query.anim_time*360)*-5",
						"math.sin(query.anim_time*(360+36*math.clamp(query.ground_speed*25,0,10)))*(2.5+0.25*math.clamp(query.ground_speed*25,0,10))",
						"math.sin(query.anim_time*(360+36*math.clamp(query.ground_speed*25,0,10))+60)*-5"
					],
					"position": ["math.sin(query.anim_time*(360+36*math.clamp(query.ground_speed*25,0,10))-60)*-0.1", "0.5+math.sin(query.anim_time*360-60)*0.25", 0]
				},
				"tail": {
					"rotation": [
						0,
						"math.sin(query.anim_time*(360+36*math.clamp(query.ground_speed*25,0,10))+120)*(10-4*math.clamp(query.ground_speed*25,0,10))",
						"math.sin(query.anim_time*(360+36*math.clamp(query.ground_speed*25,0,10))+180)*-1*math.clamp(query.ground_speed*25,0,10)"
					]
				},
				"fin_right": {
					"rotation": [0, 0, "math.sin(query.anim_time*(360+36*math.clamp(query.ground_speed*25,0,10)))*-30"]
				},
				"fin_left": {
					"rotation": [0, 0, "math.sin(query.anim_time*(360+36*math.clamp(query.ground_speed*25,0,10)))*30"]
				},
				"spikes": {
					"scale": "0.95+0.05*math.clamp(query.ground_speed*25,0,1)+(math.sin(query.anim_time*720)*-0.025+math.sin(query.anim_time*1440)*0.025)*math.clamp(query.ground_speed*25-1,-1,0)"
				}
			}
		}
	}
}