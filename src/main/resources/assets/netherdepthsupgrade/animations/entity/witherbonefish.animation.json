{
	"format_version": "1.8.0",
	"animations": {
		"witherbonefish.moving": {
			"loop": true,
			"animation_length": 8,
			"bones": {
				"BodyBack": {
					"rotation": [
						0,
						"math.sin(query.anim_time*360+60)*-20*math.clamp(query.ground_speed*25,0,1)",
						"math.sin(query.anim_time*360)*5*math.clamp(query.ground_speed*25,0,1)"
					],
					"position": [0, 0, 0]
				},
				"Fin": {
					"rotation": [
						0,
						"math.sin(query.anim_time*360+30)*-20*math.clamp(query.ground_speed*25,0,1)",
						"math.sin(query.anim_time*360-30)*10*math.clamp(query.ground_speed*25,0,1)"
					],
					"position": [0, 0, 0]
				},
				"BodyFront": {
					"rotation": [
						0,
						"math.sin(query.anim_time*360)*10*math.clamp(query.ground_speed*25,0,1)",
						"math.sin(query.anim_time*360-60)*-10*math.clamp(query.ground_speed*25,0,1)"
					],
					"position": ["math.sin(query.anim_time*360-90)*-1*math.clamp(query.ground_speed*25,0,1)", 0, 0]
				},
				"Head": {
					"rotation": [
						0,
						"math.sin(query.anim_time*360-60)*-2.5*math.clamp(query.ground_speed*25,0,1)",
						"math.sin(query.anim_time*360-120)*2.5*math.clamp(query.ground_speed*25,0,1)"
					],
					"position": ["math.sin(query.anim_time*360-60)*-1*math.clamp(query.ground_speed*25,0,1)", 0, 0]
				},
				"Main": {
					"rotation": [0, "math.sin(query.anim_time*180+60)*(-5+5*math.clamp(query.ground_speed*25,0,1))", "-45+45*math.clamp(query.ground_speed*25,0,1)"],
					"position": [0, "math.sin(query.anim_time*180)*1", 0]
				}
			}
		}
	},
	"geometry.animations": {
		"texturewidth": 64,
		"textureheight": 64
	}
}