{
	"format_version": "1.8.0",
	"animations": {
		"obsidianfish.idle": {
			"loop": true,
			"animation_length": 8,
			"bones": {
				"Head": {
					"rotation": [
						0,
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))-60)*(-2.5-0.25*math.clamp(query.ground_speed*50,0,30))",
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))-60)*5"
					]
				},
				"Upperfin": {
					"rotation": [
						"-30+math.clamp(query.ground_speed*50,0,30)",
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))+30)*(-2.5-0.1*math.clamp(query.ground_speed*50,0,30))",
						0
					],
					"position": [0, 0, 0]
				},
				"Lowerfin": {
					"rotation": [
						"20-math.clamp(query.ground_speed*50,0,20)",
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))+30)*(-2.5-0.1*math.clamp(query.ground_speed*50,0,30))",
						0
					]
				},
				"MidBody": {
					"rotation": [
						0,
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1)))*(2.5+0.25*math.clamp(query.ground_speed*50,0,30))",
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))-60)*-5"
					],
					"position": ["math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))-120)*(-0.5-0.025*math.clamp(query.ground_speed*50,0,30))", 0, 0]
				},
				"BackBody": {
					"rotation": [0, "math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))+60)*(-5-0.5*math.clamp(query.ground_speed*50,0,30))", 0]
				},
				"Backfin": {
					"rotation": [
						0,
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1)))*(-10-0.5*math.clamp(query.ground_speed*50,0,30))",
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))-60)*(10-math.clamp(query.ground_speed*50,0,10))"
					]
				},
				"Leftfin": {
					"rotation": [
						"math.sin(query.anim_time*360-60)*(-20+math.clamp(query.ground_speed*50,0,20))",
						"math.clamp(query.ground_speed*50,0,30)+math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1)))*-0.25*math.clamp(query.ground_speed*50,0,30)",
						"-45+85*math.clamp(query.ground_speed*50,0,1)+math.sin(query.anim_time*360)*(10-math.clamp(query.ground_speed*50,0,10))"
					]
				},
				"Rightfin": {
					"rotation": [
						"math.sin(query.anim_time*360-60)*(-20+math.clamp(query.ground_speed*50,0,20))",
						"-math.clamp(query.ground_speed*50,0,30)+math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1)))*-0.25*math.clamp(query.ground_speed*50,0,30)",
						"45-85*math.clamp(query.ground_speed*50,0,1)+math.sin(query.anim_time*360)*(-10+math.clamp(query.ground_speed*50,0,10))"
					]
				}
			}
		},
		"obsidianfish.moving": {
			"loop": true,
			"animation_length": 8,
			"bones": {
				"Head": {
					"rotation": [
						0,
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))-60)*(-2.5-0.25*math.clamp(query.ground_speed*50,0,30))",
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))-60)*5"
					]
				},
				"Upperfin": {
					"rotation": [
						"-30+math.clamp(query.ground_speed*50,0,30)",
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))+30)*(-2.5-0.1*math.clamp(query.ground_speed*50,0,30))",
						0
					],
					"position": [0, 0, 0]
				},
				"Lowerfin": {
					"rotation": [
						"20-math.clamp(query.ground_speed*50,0,20)",
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))+30)*(-2.5-0.1*math.clamp(query.ground_speed*50,0,30))",
						0
					]
				},
				"MidBody": {
					"rotation": [
						0,
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1)))*(2.5+0.25*math.clamp(query.ground_speed*50,0,30))",
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))-60)*-5"
					],
					"position": ["math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))-120)*(-0.5-0.025*math.clamp(query.ground_speed*50,0,30))", 0, 0]
				},
				"BackBody": {
					"rotation": [0, "math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))+60)*(-5-0.5*math.clamp(query.ground_speed*50,0,30))", 0]
				},
				"Backfin": {
					"rotation": [
						0,
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1)))*(-10-0.5*math.clamp(query.ground_speed*50,0,30))",
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))-60)*(10-math.clamp(query.ground_speed*50,0,10))"
					]
				},
				"Leftfin": {
					"rotation": [
						"math.sin(query.anim_time*360-60)*(-20+math.clamp(query.ground_speed*50,0,20))",
						"math.clamp(query.ground_speed*50,0,30)+math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1)))*-0.25*math.clamp(query.ground_speed*50,0,30)",
						"-45+85*math.clamp(query.ground_speed*50,0,1)+math.sin(query.anim_time*360)*(10-math.clamp(query.ground_speed*50,0,10))"
					]
				},
				"Rightfin": {
					"rotation": [
						"math.sin(query.anim_time*360-60)*(-20+math.clamp(query.ground_speed*50,0,20))",
						"-math.clamp(query.ground_speed*50,0,30)+math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1)))*-0.25*math.clamp(query.ground_speed*50,0,30)",
						"45-85*math.clamp(query.ground_speed*50,0,1)+math.sin(query.anim_time*360)*(-10+math.clamp(query.ground_speed*50,0,10))"
					]
				}
			}
		},
		"obsidianfish.flee": {
			"loop": true,
			"animation_length": 8,
			"bones": {
				"Head": {
					"rotation": [
						0,
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))-60)*(-2.5-0.25*math.clamp(query.ground_speed*50,0,30))",
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))-60)*5"
					]
				},
				"Upperfin": {
					"rotation": [
						"-30+math.clamp(query.ground_speed*50,0,30)",
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))+30)*(-2.5-0.1*math.clamp(query.ground_speed*50,0,30))",
						0
					],
					"position": [0, 0, 0]
				},
				"Lowerfin": {
					"rotation": [
						"20-math.clamp(query.ground_speed*50,0,20)",
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))+30)*(-2.5-0.1*math.clamp(query.ground_speed*50,0,30))",
						0
					]
				},
				"MidBody": {
					"rotation": [
						0,
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1)))*(2.5+0.25*math.clamp(query.ground_speed*50,0,30))",
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))-60)*-5"
					],
					"position": ["math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))-120)*(-0.5-0.025*math.clamp(query.ground_speed*50,0,30))", 0, 0]
				},
				"BackBody": {
					"rotation": [0, "math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))+60)*(-5-0.5*math.clamp(query.ground_speed*50,0,30))", 0]
				},
				"Backfin": {
					"rotation": [
						0,
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1)))*(-10-0.5*math.clamp(query.ground_speed*50,0,30))",
						"math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1))-60)*(10-math.clamp(query.ground_speed*50,0,10))"
					]
				},
				"Leftfin": {
					"rotation": [
						"math.sin(query.anim_time*360-60)*(-20+math.clamp(query.ground_speed*50,0,20))",
						"math.clamp(query.ground_speed*50,0,30)+math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1)))*-0.25*math.clamp(query.ground_speed*50,0,30)",
						"-45+85*math.clamp(query.ground_speed*50,0,1)+math.sin(query.anim_time*360)*(10-math.clamp(query.ground_speed*50,0,10))"
					]
				},
				"Rightfin": {
					"rotation": [
						"math.sin(query.anim_time*360-60)*(-20+math.clamp(query.ground_speed*50,0,20))",
						"-math.clamp(query.ground_speed*50,0,30)+math.sin(query.anim_time*(180+180*math.clamp(query.ground_speed*50,0,1)))*-0.25*math.clamp(query.ground_speed*50,0,30)",
						"45-85*math.clamp(query.ground_speed*50,0,1)+math.sin(query.anim_time*360)*(-10+math.clamp(query.ground_speed*50,0,10))"
					]
				}
			}
		}
	}
}